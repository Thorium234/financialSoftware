/**
 * Active academic term for the frontend.
 * Keep in sync with the server's lib/constants.ts when the term changes.
 */
export const CURRENT_YEAR = "2025";
export const CURRENT_TERM = 2;
export const CURRENT_TERM_LABEL = `Term ${CURRENT_TERM} ${CURRENT_YEAR}`;
